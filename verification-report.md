# Engineering Knowledge Base 公开候选仓库核验报告

> 历史快照：本报告记录首次公开前的核验状态，其中“远程仓库尚未创建”等表述仅描述当时事实，不是当前状态。

## 1. 结论

结论：**PUBLIC_READY**

候选仓库的公开内容、事实边界、图片、PDF、相对链接、敏感信息和桌面/手机静态页视觉检查均已通过。GitHub login 与数字 ID 已由用户明确确认，仓库级提交身份使用 GitHub noreply 邮箱。

本结论只表示本地公开候选内容已具备独立复核条件。远程仓库当前尚未创建，本轮没有发布到互联网。

## 2. 公开定位与版本事实

- 仓库性质：公开项目展示仓库；
- 当前公开内容：项目介绍与界面演示；
- 暂未公开：完整源码和可安装版本；
- 已有本地可运行 MVP；
- v0.1.2 为稳定展示基线；
- v0.1.3 正在进行稳定性修复、扩大真实资料规模和完整人工测试，尚未完成、尚未发布；
- 长期 AI、手写识别与局部解释能力均明确标为规划，不是当前功能。

版本事实已与本地 `v0.1.2` annotated tag、发布说明、人工测试结果和配置文件交叉核验。仓库中可确认 v0.1.0、v0.1.1、v0.1.2 三个版本 tag，支持“已持续迭代多个版本”的表述。

## 3. 文本与结构检查

### README

- 1 个一级标题、10 个二级标题、8 个三级标题；
- 7 张截图均有 alt 文本，并限制展示宽度；
- 所有图片相对链接存在；
- 项目介绍 PDF 相对链接存在；
- Markdown 已使用本地解析器渲染，无残留未解析链接语法；
- 未使用“开源项目”表述，也未添加开放许可证。

### 静态页

- `lang="zh-CN"`；
- UTF-8 编码声明存在，中文文件可正常解码；
- 所有 7 张图片和 PDF 相对链接存在；
- 无外部 HTTP/HTTPS 资源；
- 无外部字体、JavaScript、统计脚本、iframe、表单或追踪；
- 图片使用原始宽高属性和自适应 CSS，不强制拉伸；
- 已提供桌面、平板和手机断点；
- 使用仅监听 `127.0.0.1:8514` 的临时静态服务器完成浏览器运行时检查；
- 桌面端 1440 × 900：中文、导航、标题、段落和卡片显示正常，无横向溢出；
- 手机端 390 × 844：内容按单列排列，无横向溢出，正文 16 px、主标题 42 px、界面卡片标题 21 px；
- 滚动到界面区域后 7 张图片全部加载，未发现变形或破图；
- PDF 按钮及 README、图片、PDF 的本地 HTTP 请求均返回 200；
- 页面未显示本地路径、用户名占位符或内部信息，浏览器控制台无错误或警告；
- 临时核验截图未加入公开候选文件；
- 检查完成后临时服务进程已结束，8514 端口不再监听。

### 项目介绍 PDF

- 5 页，页面尺寸 960 × 540 pt（16:9）；
- 未加密；
- 无 JavaScript、打开动作、附加动作、嵌入文件或外部 URI；
- 中文文本可提取；
- 已以 144 DPI 渲染为 5 张 1920 × 1080 图片逐页复核；
- 未发现裁切、乱码、文字溢出、截图变形或明显低清问题；
- 输出文件与已核验申请附件 SHA-256 完全一致。

附件中“尚未公开发布”指完整源码和可安装版本尚未公开；README 和静态页已明确说明，不把项目介绍页面描述为未公开。

## 4. 二进制文件清单与来源

所有二进制文件均来自已核验的申请附件目录；未从原工程仓库复制任何业务文件。

| 输出文件 | 来源文件 | 像素或页数 | 大小（字节） | 处理 |
| --- | --- | ---: | ---: | --- |
| `assets/home.png` | `01-首页概览.png` | 1280 × 720 | 204,838 | 解码后重新保存为真实 PNG，清除元数据 |
| `assets/reading-and-review.png` | `02-阅读与页面状态.png` | 1280 × 720 | 172,051 | 同上 |
| `assets/search-overview.png` | `03-搜索与批量整理.png` | 1280 × 720 | 208,500 | 同上 |
| `assets/batch-operations.png` | `04-检索高亮与批量操作.png` | 1280 × 720 | 160,508 | 同上 |
| `assets/search-highlight.png` | `05-关键词高亮结果.png` | 1280 × 720 | 417,078 | 同上 |
| `assets/evidence-basket.png` | `06-证据篮与备注.png` | 1280 × 720 | 178,501 | 同上 |
| `assets/maintenance.png` | `07-本地备份与维护.png` | 1280 × 510 | 146,342 | 同上 |
| `docs/project-introduction.pdf` | 已核验项目介绍 PDF | 5 页 | 899,384 | 原样复制，哈希一致 |

7 张输出图片均为 PNG 编码，图片信息字典和 EXIF 为空；尺寸和宽高比与来源一致。

PDF 元数据只包含项目附件标题、浏览器生成器、PDF 生成器和生成时间，不包含用户名、邮箱、绝对路径或认证信息。

## 5. 隐私与公开安全扫描

扫描覆盖全部文本文件、文件名、PDF 提取文本、PDF 元数据、PDF 动作与 URI、图片格式、图片信息字典和 EXIF。

未发现：

- 本地盘符路径、用户目录、系统用户名或应用数据目录；
- 未关闭的演示或核验服务；
- 内部编号或内部路线图内容；
- 邮箱、手机号、账号、密码、密钥或认证信息；
- 真实数据库文件、WAL、SHM、备份实例或日志；
- GitHub 认证信息或已配置的远程地址；
- 他人姓名、群聊、私人关系或内部测试缺陷细节；
- 真实课程资料、个人知识库正文或第三方 PDF。

预期命中及解释：

| 命中 | 位置 | 解释 |
| --- | --- | --- |
| `127.0.0.1:8501` | README、静态页 | 这是产品仅监听本机回环地址的网络边界，不是本地文件路径或隐私泄露 |
| `127.0.0.1:8514` | 本报告 | 这是本轮静态页视觉检查使用的临时本机端口，检查后已关闭 |
| `API Key` | README、静态页、PDF | 用于说明 v0.1.2 无需 API Key，没有任何实际密钥值 |
| `.env`、数据库扩展名 | `.gitignore` | 只作为防止误提交私密运行数据的忽略规则，仓库中不存在这些文件 |
| `JZ-05T68` 与未来仓库地址 | 申请答案、发布计划、本报告 | 已由用户确认的 GitHub login 和计划公开地址；远程仓库当前尚未创建 |

PDF 中包含申请人姓名和宽泛的专业身份，这是用户提供的申请附件中的预期公开身份信息；未包含联系方式、学校、住址或其他敏感身份信息。

## 6. 版权与许可证边界

- 截图来自项目自身实际界面；
- 截图内资料为自行生成的合成演示内容；
- 未复制第三方真实 PDF、课程资料或作品；
- `NOTICE.md` 已说明当前未授予复制、再分发或商用许可；
- 未添加 MIT、Apache、GPL、CC 或其他开放许可证；
- 第三方产品名称和商标权利归属已克制说明。

未发现阻断性的版权风险。

## 7. 内容边界检查

候选仓库不包含：

- 完整业务源码或测试源码；
- 原工程 Git 历史；
- 数据库、WAL、SHM、备份、日志或真实资料；
- 内部路线图、内部测试计划、具体缺陷报告或内部安装入口；
- 浏览器配置、环境文件、认证信息或用户目录；
- v0.1.3 具体缺陷细节。

当前功能、未来 30 天计划和长期方向已分别呈现，没有把规划写成已实现功能。

## 8. Git 与发布状态

- GitHub login 已由用户确认为 `JZ-05T68`；
- GitHub 数字 ID 已由用户确认为 `271708767`；
- 仓库级 `user.name` 为 `YANG YANG`；
- 仓库级 `user.email` 使用已确认账号的 GitHub noreply 地址，具体地址只存在于本地 `.git/config` 和提交元数据，不写入公开正文；
- 全局 Git 用户名和邮箱在设置前后保持不变；
- 分支为 `main`，候选目录是独立本地 Git 仓库；
- 最终提交信息为 `chore: prepare public project showcase`；
- 最终提交 SHA 由本报告所在提交本身决定，无法在不改变 SHA 的前提下写入其自身；提交后使用 `git rev-parse HEAD` 核验，精确值记录在最终交付汇报中；
- 工作树干净，远程地址为空；
- 未创建远程仓库、未推送、未启用 GitHub Pages、未公开任何内容。

未来申请链接：

```text
https://github.com/JZ-05T68/engineering-knowledge-base
```

该远程仓库当前尚未创建，本轮未公开。

## 9. 原工程仓库不变性

任务开始和收口时，原工程仓库保持相同分支、HEAD 和工作区状态。本任务没有编辑、暂存或提交原工程中的任何文件，也没有复制其 `.git` 目录或历史。

## 10. 验证说明

本任务未修改原工程业务代码，因此没有在原工程运行 Ruff 或 Pytest；功能事实采用 v0.1.2 已有发布材料和代码只读核验。本轮实际验证覆盖 Markdown 渲染、HTML 结构、相对链接、UTF-8、桌面与手机浏览器视觉检查、图片尺寸与元数据、PDF 文本与动作、敏感信息扫描、文件清单、体积和 Git 状态。
